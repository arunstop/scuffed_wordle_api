{
  "version": "0.2.0",
  "configurations": [
      {
          "command": "npm run start:dev",
          "name": "Start dev mode",
          "request": "launch",
          "type": "node-terminal"
      },

  ]
}
